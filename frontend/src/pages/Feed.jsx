import {useEffect,useState} from 'react'; import {Search,SlidersHorizontal,BrainCircuit} from 'lucide-react'; import api from '../services/api'; import SkillCard from '../components/SkillCard';
export default function Feed(){const [skills,setSkills]=useState([]);const [search,setSearch]=useState('');const [category,setCategory]=useState('');const [type,setType]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);api.get('/skills',{params:{search,category,skill_type:type}}).then(r=>setSkills(r.data)).finally(()=>setLoading(false))},[search,category,type]);return <main className="container"><div className="pageintro"><div><div className="eyebrow"><BrainCircuit size={15}/> Discover</div><h1>Find your next skill exchange</h1><p>Search real people offering and requesting skills.</p></div></div><div className="filters card"><div className="search"><Search size={18}/><input placeholder="Search React, design, Python…" value={search} onChange={e=>setSearch(e.target.value)}/></div><select value={category} onChange={e=>setCategory(e.target.value)}><option value="">All categories</option><option>Technology</option><option>Design</option><option>Languages</option><option>Communication</option><option>Business</option><option>Creative</option></select><select value={type} onChange={e=>setType(e.target.value)}><option value="">Offer + request</option><option value="Offering">Offering</option><option value="Requesting">Requesting</option></select><SlidersHorizontal size={18}/></div>{loading?<div className="loading">Loading opportunities…</div>:<div className="grid">{skills.map(s=><SkillCard key={s.id} skill={s}/>)}</div>}{!loading&&!skills.length&&<div className="empty card"><h3>No matches yet</h3><p>Try a broader search or another category.</p></div>}</main>}
