import {useEffect,useState} from 'react'; import {Trophy,MapPin,Star,Flame} from 'lucide-react'; import api from '../services/api';
export default function Leaderboard(){const [rows,setRows]=useState([]);useEffect(()=>{api.get('/leaderboard').then(r=>setRows(r.data))},[]);return <main className="container"><div className="pageintro"><div><div className="eyebrow"><Trophy size={15}/> Community leaderboard</div><h1>Learn. Teach. Level up.</h1><p>Earn XP by sharing skills, completing exchanges and building trust.</p></div></div><div className="leaderboard card">{rows.map((r,i)=><article className={'leaderrow '+(i<3?'toprank':'')} key={r.user_id}><div className="rank">{i<3?['🥇','🥈','🥉'][i]:i+1}</div><div className="avatar">{r.name[0]}</div><div className="leadername"><strong>{r.name}</strong><span><MapPin size={13}/>{r.location||'Remote'}</span></div><div className="leaderstat"><span>Level</span><b>{r.level}</b></div><div className="leaderstat"><span>Skills</span><b>{r.skills}</b></div><div className="leaderstat"><span>Swaps</span><b>{r.completed}</b></div><div className="leaderxp"><Flame size={15}/> {r.xp} XP</div><div className="leaderating"><Star size={14}/> {r.rating||'—'}</div></article>)}{!rows.length&&<div className="empty">Loading community rankings…</div>}</div></main>}
