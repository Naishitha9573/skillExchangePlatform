import {useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import {ArrowRight,Plus,BrainCircuit,ArrowUpRight,Users,Star,Clock3,CheckCircle2} from 'lucide-react'; import api from '../services/api'; import SkillCard from '../components/SkillCard'; import {useAuth} from '../context/AuthContext';
export default function Dashboard(){const {user}=useAuth();const [stats,setStats]=useState({});const [rec,setRec]=useState([]);const [skills,setSkills]=useState([]);useEffect(()=>{Promise.all([api.get('/dashboard'),api.get('/recommendations'),api.get('/skills')]).then(([a,b,c])=>{setStats(a.data);setRec(b.data);setSkills(c.data.filter(x=>x.owner_id===user.id).slice(0,4))}).catch(console.error)},[user.id]);return <main className="container"><section className="dashhero"><div><div className="eyebrow"><BrainCircuit size={15}/> Your AI learning dashboard</div><h1>Hi, {user.full_name.split(' ')[0]} 👋</h1><p>Exchange knowledge, grow your network and let AI surface the best people to learn from.</p></div><Link className="button" to="/add-skill"><Plus size={18}/> Add a skill</Link></section><div className="gamify card"><div className="levelcircle"><span>LVL</span><strong>{stats.level||1}</strong></div><div className="xpinfo"><div className="sectionhead"><div><h2>Community progress</h2><p>{stats.xp||0} XP earned · {stats.streak||0} day activity streak</p></div><span className="match">{Math.min(100,Math.round(((stats.xp||0)%300)/3))}% to next level</span></div><div className="progressbar"><span style={{width:`${Math.min(100,Math.round(((stats.xp||0)%300)/3))}%`}}></span></div><div className="badges">{(stats.badges||[]).map(b=><span title={b.description} key={b.id}>{b.icon} {b.name}</span>)}{!(stats.badges||[]).length&&<span>✨ Complete swaps to unlock badges</span>}</div></div></div><div className="stats">{[['Your offers',stats.offered,Users],['Learning goals',stats.requested,BrainCircuit],['Pending swaps',stats.incoming+stats.outgoing,Clock3],['Completed',stats.completed,CheckCircle2],['Rating',stats.rating||'—',Star]].map(([t,v,I])=><div className="stat card" key={t}><I size={20}/><div><span>{t}</span><strong>{v}</strong></div></div>)}</div><section className="section"><div className="sectionhead"><div><h2>AI recommendations</h2><p>People and skills with the strongest complementary fit.</p></div><Link to="/feed">Explore all <ArrowRight size={16}/></Link></div>{rec.length?<div className="grid">{rec.slice(0,3).map(x=><SkillCard key={x.skill.id} skill={x.skill} match={x.score}/>)}</div>:<div className="empty card"><BrainCircuit size={30}/><h3>Add your first skill to unlock AI matches</h3><p>Post something you can teach or want to learn.</p><Link className="button small" to="/add-skill">Add skill</Link></div>}</section><section className="section"><div className="sectionhead"><div><h2>Your skills</h2><p>Keep your exchange profile current.</p></div><Link to="/profile">Manage profile <ArrowUpRight size={16}/></Link></div>{skills.length?<div className="grid">{skills.map(x=><SkillCard key={x.id} skill={x} own/>)}</div>:<div className="empty card"><p>No skills yet.</p><Link to="/add-skill" className="button small">Create your first skill</Link></div>}</section></main>}
