import {Link} from 'react-router-dom'; import {ArrowUpRight,ShieldCheck,Sparkles} from 'lucide-react';
export default function SkillCard({skill,match,reason,reasons,breakdown,signals,own=false}){return <article className="card skillcard"><div className="cardtop"><span className={skill.type==='Offering'?'pill green':'pill blue'}>{skill.type}</span>{match!=null&&<span className="match">{match}% compatibility</span>}</div><h3>{skill.title}</h3><p>{skill.description}</p><div className="tags">{skill.tags?.split(',').filter(Boolean).slice(0,3).map(x=><span key={x}>#{x.trim()}</span>)}</div><div className="skillmeta"><span><strong>{skill.owner?.full_name||'You'}</strong></span><span>{skill.level}</span></div>{reason&&<div className="matchreason"><Sparkles size={13}/><span>{reason}</span></div>}{signals&&<small className="matchsource">{signals.collaborative?'Based on similar successful exchanges':'Cold-start content and profile matching'}{signals.rating>0?` · ${signals.rating}/5 reputation`:''}</small>}{!own&&<Link className="cardaction" to={`/skill/${skill.id}`}>View opportunity <ArrowUpRight size={16}/></Link>}</article>}
