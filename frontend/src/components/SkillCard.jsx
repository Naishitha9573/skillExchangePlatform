import {Link} from 'react-router-dom'; import {ArrowUpRight} from 'lucide-react';
export default function SkillCard({skill,match,own=false}){return <article className="card skillcard"><div className="cardtop"><span className={skill.type==='Offering'?'pill green':'pill blue'}>{skill.type}</span>{match!=null&&<span className="match">{match}% match</span>}</div><h3>{skill.title}</h3><p>{skill.description}</p><div className="tags">{skill.tags?.split(',').filter(Boolean).slice(0,3).map(x=><span key={x}>#{x.trim()}</span>)}</div><div className="skillmeta"><span><strong>{skill.owner?.full_name||'You'}</strong></span><span>{skill.level}</span></div>{!own&&<Link className="cardaction" to={`/skill/${skill.id}`}>View opportunity <ArrowUpRight size={16}/></Link>}</article>}
