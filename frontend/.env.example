VITE_API_URL=http://localhost:8000/api/v1
# The WebSocket URL is derived from VITE_API_URL, including its path prefix.
# All secrets, Google credentials, and TURN configuration belong on the backend.
